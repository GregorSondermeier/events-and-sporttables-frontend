import { EventsCategoriesIdsToStringPipe } from './events-categories-ids-to-string.pipe';

describe('EventsCategoriesIdsToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new EventsCategoriesIdsToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
